import CatalogSync from "../ui/catalog/CatalogSync";

// тут будет получение данных (все аккумуляторы)

export default function Catalog() {
  return (
    <section>
      <CatalogSync />
    </section>
  );
}
